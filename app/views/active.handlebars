<!-- activities playlist screen -->
<div class="show" id="active">
  <div class="row">
    <ul id="activities-list">
      {{#each playlist}}
      <li>
        <a href="{{this.LINK}}">
          <img src="{{this.IMAGE}}">
        </a>
        <h5 class="licard-title">{{this.NAME}}</h5>


        <form action="/playlist/activities/like/{{this.id}}" method="POST">
          <input type="hidden" name="rating">
          <button class="btn" type="submit">
            <h2><i class="fa fa-thumbs-o-up"> {{this.LIKED}}</i></h2>
          </button>
        </form>

        <form action="/playlist/activities/dislike/{{this.id}}" method="POST">
          <input type="hidden" name="rating">
          <button class="btn" type="submit">
            <h2><i class="fa fa-thumbs-o-down"> {{this.DISLIKED}}</i></h2>
          </button>
        </form>


      </li>
      {{/each}}
    </ul>
    </a>
  </div>
</div>